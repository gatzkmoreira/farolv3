{
    "name": "farol-backend",
    "version": "0.1.0",
    "description": "FarolV3 Backend Core - Contracts and utilities for migration",
    "main": "src/index.ts",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "build": "tsc",
        "lint": "eslint src/"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "vitest": "^1.2.0",
        "@types/node": "^20.10.0"
    },
    "engines": {
        "node": ">=18"
    }
}